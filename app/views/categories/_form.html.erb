<%= simple_form_for @category, defaults: { input_html: { class: 'form-control form-horizontal' } } do |f| %>
    <%= f.input :name %><br/>
    <%= f.association :user, label_method: :full_name, selected: current_user.id %>
    <%= f.button :submit %>
<% end %>
